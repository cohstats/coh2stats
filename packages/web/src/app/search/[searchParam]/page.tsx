"use client";

import CustomSearch from "../../../components/search/search";

// Force dynamic rendering
export const dynamic = "force-dynamic";

const SearchWithParamPage = () => {
  return <CustomSearch />;
};

export default SearchWithParamPage;
